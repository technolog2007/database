1.Згенерувати за допомогою Streams N POJO-меседжів 
(N задається як аргумент запуску (System.properties),
N > 1000
pojo: {name: “текст”,
       count: xxx,
       created_at: date_time}
       рандомні)

2.І відправити їх у одну чергу ActiveMq <IN-черга>, 
Коннект/назву черги винести у properties,

3.Стоп генерації - по часу (параметр в секундах із проперті) через "poison pill"

4. Прочитати месседжі із IN-черги (одночасно з генерацією кому цікаво паралельна робота чи після генерації,
для простоти),

прогнати через валідатори

5. Валідні записати у один csv-файл із двома полями name & count

6. Невалідні - у інший csv файл із додатковим полем errors у вигляді json {“errors”:[]}.

7. Додаткове завдання для тих, кому стане скучно: запустіть 2-5 сервісів на різних AWS
(найкраще одногрупника) щоб кілька сервісів заповнювали чергу, а один вичитував.

(довжина name >=7, є хоча б одна буква ‘a’, count >= 10)+